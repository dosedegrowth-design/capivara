import { EmConstrucao } from "@/components/capivara/em-construcao";

export default function Page() {
  return <EmConstrucao
    area="LGPD"
    descricao="Solicitações de acesso, anonimização e portabilidade de dados."
    voltarHref="/admin"
    voltarLabel="Voltar pra visão geral"
  />;
}
